import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#8774e1",
            dark: "hsl(250.45871559633028, 64.49704142011832%, 62.86274509803921%)",
            light: "rgba(135, 116, 225, 0.08)",
            contrastText: "#fff",
        },
    },
});

export default theme;
